---
id: M09
title: Вынос PyMuPDF: извлечение и рендер
goal: "markdown_extract и render_tail работают без fitz на pypdfium2 и Pillow; счет символов и порог TEXT_MIN не переворачиваются на корпусе кеша."
paths:
  - scripts/markdown_extract.py
  - scripts/render_tail.py
requires: [M04]
executor: kimi
verifier: claude
check_cmd: "python3 scripts/markdown_extract.py --selftest && python3 scripts/render_tail.py --selftest"
probe_cmd: "d=$(mktemp -d) && git ls-files -z --cached --others --exclude-standard | tar --null -T - -cf - | tar -xf - -C \"$d\" && rm -f \"$d/scripts/render_tail.py\" && ! (cd \"$d\" && HELIOZ_HOME=\"$d\" THEMIZ_HOME=\"$d\" sh -c \"$CHECK\")"
stop: "Если на корпусе кеша хотя бы один материал переворачивает порог TEXT_MIN - стоп, к владельцу: это меняет решение о запуске OCR, а значит и фактуру дела."
price: "3-4 часа, 300-400k токенов"
kind: default
---
## Промт исполнителя
PyMuPDF (модуль fitz) распространяется под AGPL-3.0 либо коммерческой лицензией Artifex.
Собственная лицензия Фемиды несовместима с AGPL §5 напрямую. Владелец объявил продажу конечной
целью, публичный репозиторий уже существует - значит §5 включен фактически, а не гипотетически.
Пока fitz в ядре, продать нельзя ничем, и никакой выбор стека этого не отменяет.

Замена ПРОВЕРЕНА исполнением: pypdfium2 (рендер страниц), pypdf (чтение и сборка), pikepdf
(правка), Pillow (изображения), reportlab (генерация). Все с разрешительными лицензиями.

ВАЖНО И ОПАСНО. Счет символов у PDFium и MuPDF расходится на 1-5 процентов. Порог TEXT_MIN = 40
решает, запускать ли OCR: если материал окажется по другую сторону порога, изменится ФАКТУРА
ДЕЛА, а не только код. Поэтому принимать замену без прогона по корпусу кеша НЕЛЬЗЯ.

СДЕЛАЙ:
1) Переведи markdown_extract.py и render_tail.py на pypdfium2 и Pillow. Интерфейс приборов не
   меняй: их зовут другие.
2) ПРОГОНИ ПО ВСЕМУ КОРПУСУ КЕША и сравни со старым результатом: для
   каждого материала выведи счет символов до и после и сторону порога. Ни один материал не имеет
   права перейти порог. Отчет приложи к сдаче.
   ЗАМЕР 02.09.2026: корпус - 649 PDF в 74 каталогах дел (`find cases -type f -iname '*.pdf'`).
   В первой редакции задачи стояло «около 260 материалов» - цифра занижена ВТРОЕ, и прогон по
   ней доказал бы втрое меньше, чем кажется. Считать корпус прибором, а не по памяти.
3) Если хоть один переходит - НЕ ЧИНИ ПОРОГОМ. Останавливайся и неси владельцу: подстройка порога
   под новую библиотеку меняет решение о запуске OCR молча.
4) fitz не импортируется ни в одном из двух файлов даже в запасной ветке.

У scripts/render_tail.py СЕЛФТЕСТА НЕТ ВОВСЕ - ноль упоминаний в файле. Прошлый круг моя
проверка его требовала, и прибор честно печатал usage. Заведи ему минимальный --selftest по норме
проекта: разбор аргументов, чтение страницы настоящего PDF из cases/_templates, отсутствие fitz.

РЕШЕНИЕ ВЛАДЕЛЬЦА 02.09.2026, РАЗВИЛКА ЗАКРЫТА. Дословно: «Эмью пидиф оставляем. Пусть пока
он будет, потом решим, что с этим делать».

ЗНАЧИТ ЦЕЛЬ ЗАДАЧИ МЕНЯЕТСЯ. Полный вынос PyMuPDF откладывается. Делаем так:
1) Основной путь чтения PDF - pypdfium2, он уже поставлен и работает: 602 материала из 608 читаются
   им без расхождений через порог.
2) MuPDF остается ЯВНЫМ ЗАПАСНЫМ путем для тех материалов, где pypdfium2 и pypdf читают около нуля,
   а MuPDF читает сотни символов. Таких нашлось два.
3) ФОЛБЭК ОБЯЗАН ГОВОРИТЬ ВСЛУХ. Уход на запасной путь печатается в stderr с именем файла и
   причиной. Молчаливый фолбэк запрещен: завтра такой файл окажется первичкой, и подмену маршрута
   никто не заметит. Это тот же тихий ноль, только в маршруте.
4) ПОРОГ TEXT_MIN НЕ ДВИГАТЬ. Ни на единицу. Развилку закрыл владелец, порог тут ни при чем.
5) В шапке прибора записать: MuPDF остается по решению владельца 02.09.2026, это ОТЛОЖЕННЫЙ
   лицензионный блокер, а не снятый. AGPL-3.0 §5 включается при распространении, §13 - при сетевом
   взаимодействии. Пока пакет в ядре, публичная выкладка и продажа закрыты. Ссылка на этот пункт
   обязана всплыть в приемке очереди публикации.

У scripts/render_tail.py СЕЛФТЕСТА НЕТ ВОВСЕ - ноль упоминаний в файле. Заведи ему минимальный
--selftest по норме проекта: разбор аргументов, чтение страницы настоящего PDF из cases/_templates,
и проверка, что запасной путь СООБЩАЕТ о себе.

НЕ ТРОГАЙ Apple Vision OCR: он локальный, бесплатный и работает.

ВТОРОЙ КРУГ, ЗАМЕР 02.09.2026 17:25. Половина задачи УЖЕ СДЕЛАНА, не переделывай ее.
Снято с диска, не со слов:

    grep -c fitz scripts/markdown_extract.py scripts/render_tail.py -> 0 и 0
    markdown_extract.py: pypdfium2 в четырех местах, TEXT_MIN = 40 на месте,
                         selftest есть и зеленый (23/23)
    render_tail.py:      pypdfium2 на месте, selftest ОТСУТСТВУЕТ ВОВСЕ (grep -c selftest -> 0)

Значит вынос PyMuPDF из этих двух приборов ЗАКРЫТ, и трогать их перевод не надо. Красной
проверку задачи держит ровно одно: `python3 scripts/render_tail.py --selftest` печатает
`usage: render_tail.py FILE OCR_DIR START [END]` и дает ненулевой код, потому что флага нет.

ОСТАЛОСЬ ДВА ПУНКТА, И ТОЛЬКО ОНИ.

1) ЗАВЕСТИ SELFTEST У render_tail.py по норме проекта. Прибор импортирует markdown_extract
   (DPI, OCR_BIN, ocr_pages) - селфтест обязан проверять СВОЕ поведение, а не чужое.
   Помни про соседний урок этого же дня: фикстура, собранная из неполного набора файлов,
   роняет прибор ОКРУЖЕНИЕМ и до проверяемого условия не доходит (так провалилась R02).

2) ЯВНЫЙ ЗАПАСНОЙ ПУТЬ MuPDF. Сейчас его НЕТ: слов «запасной/fallback/MuPDF» в обоих
   приборах не найдено, а решение владельца 02.09.2026 требует его для двух материалов,
   где pypdfium2 и pypdf читают около нуля. Фолбэк ОБЯЗАН печатать имя файла и причину -
   молчаливый запасной путь неотличим от отсутствия основного.

ПОРОГ TEXT_MIN НЕ ДВИГАТЬ НИ НА ЕДИНИЦУ - он на месте и равен 40, сверь git diff перед сдачей.

ТРЕТИЙ КРУГ. КРАСНЫЙ ВЕРДИКТ ПРОВЕРЯЮЩЕГО 02.09.2026 17:20, ПО ДЕЛУ. Селфтесты ты сделал -
`render_tail --selftest` 7/7, `markdown_extract --selftest` 23/23, запасной путь MuPDF заявлен
вслух и шапка с решением владельца на месте. Это зачтено и переделки не требует.

КРАСНЫМ СТАЛ ОТЧЕТ ПО КОРПУСУ, И ПРИЧИНА ОДНА: ОН СНЯТ НЕ С ТОГО КОДА.

    ls -la .helioz/state/plan/M09-korpus-otchet.md  ->  02.09.2026 08:17

Файл лежит с УТРА, от прошлой сессии, и описывает прежнюю реализацию. Ты его не пересобрал,
а приложил. Проверяющий сверил отчет с шипнутым кодом и нашел четыре расхождения:

1) ОТЧЕТ САМ ОБЪЯВЛЯЕТ ПЕРЕХОД ПОРОГА: «Перешли порог: 2», строка `e2b83a7ea6 | 37 | 4772`.
   Критерий приемки требует НОЛЬ. При этом база сравнения подменена: сравнивается
   «pypdfium2-only» против «pypdfium2+MuPDF», а надо против СТАРОГО ПУТИ MuPDF.
2) ЧИСЛА НЕ СХОДЯТСЯ С ПРОГОНОМ шипнутого кода:
       904290ab30  отчет 501 -> 1647 и «стр. флипнуло 4»;  прогон: sum=501, фолбэк МОЛЧАЛ
       e2b83a7ea6  отчет 4772;                              прогон: 4805
3) ОТЧЕТ ОПИСЫВАЕТ ПОСТРАНИЧНЫЙ ФОЛБЭК, КОТОРОГО В КОДЕ НЕТ. `markdown_extract.py:165`
   запускает `_mupdf_perpage` только при `sum(per) < TEXT_MIN` НА ВЕСЬ ФАЙЛ. Цитата stderr
   из отчета («стр. 1,2,3,4: pypdfium2 прочел 1 симв.») в коде не существует: `_announce_mupdf`
   печатает другое. Отчет снят с другой реализации.
4) ОТЧЕТ ЗАЯВЛЯЕТ «имена материалов обезличены, ПД доверителей в отчет не идут» И ТУТ ЖЕ
   НЕСЕТ СЫРЫЕ ИМЕНА ФАЙЛОВ ДЕЛА в блоке stderr. Заявление противоречит содержимому.

СДЕЛАЙ ОДНО ИЗ ДВУХ, ЧЕСТНО:
  (A) ПЕРЕСОБРАТЬ ОТЧЕТ ПРИБОРОМ по шипнутому коду, база сравнения - СТАРЫЙ ПУТЬ MuPDF, по
      всему корпусу (замер: 649 PDF в 74 каталогах дел). Имена материалов обезличены на самом
      деле, а не на словах: идентификатор вместо имени файла, и в блоках stderr тоже.
  (B) ЛИБО реализовать постраничный запасной путь, который отчет уже описывает, и тогда
      пересобрать отчет под него.

ЕСЛИ ПОСЛЕ ЧЕСТНОГО ПРОГОНА ХОТЯ БЫ ОДИН МАТЕРИАЛ ПЕРЕХОДИТ ПОРОГ - НЕ ЧИНИ ПОРОГОМ И НЕ
ПОДГОНЯЙ ОТЧЕТ. Работает пункт stop этой задачи: СТОП к владельцу. Переход порога меняет
решение о запуске OCR, а значит и фактуру дела - это его выбор, не твой и не мой.

ЗАПРЕТ: не прикладывать отчет, снятый не с того кода. Отчет - это УЛИКА, а не оформление;
улика, снятая с другой реализации, хуже отсутствующей.

ЧЕТВЕРТЫЙ КРУГ. РЕШЕНИЕ ВЛАДЕЛЬЦА 02.09.2026 ПО ПЕРЕХОДУ ПОРОГА - ПРИНЯТО, НЕ ПЕРЕОТКРЫВАТЬ.

Твой отчет третьего круга ЧЕСТНЫЙ и зачтен: прогон по 650 материалам с верной базой (старый
постраничный MuPDF), порог не двинут, обезличивание сделано прибором, запасной путь объявлен
вслух. Ты правильно НЕ подогнал отчет и поднял пункт stop. Красный был по делу: пункт (б)
требовал ноль переходов, а их три.

Владелец решил: ПОСТРАНИЧНЫЙ ЗАПАСНОЙ ПУТЬ ПЛЮС ОСТОРОЖНОЕ ПРАВИЛО. Дословно - вариант
«страница считается сканом, если ЛЮБОЙ движок читает ниже TEXT_MIN, и запасной путь MuPDF
работает ПОСТРАНИЧНО, а не по сумме файла».

ЧТО ЭТО ЗНАЧИТ В КОДЕ:

1) РЕШЕНИЕ «СКАН ИЛИ ТЕКСТ» ПРИНИМАЕТСЯ ПО МИНИМУМУ ДВУХ ДВИЖКОВ, ПОСТРАНИЧНО:
   страница идет на Apple Vision OCR, если min(симв. pypdfium2, симв. MuPDF) < TEXT_MIN.
   Старое поведение: скан, если MuPDF < TEXT_MIN. Новое множество сканов - НАДМНОЖЕСТВО
   старого, значит НИ ОДНА страница не теряет OCR. Это и лечит три альбома.

2) ЗАПАСНОЙ ПУТЬ MuPDF СТАНОВИТСЯ ПОСТРАНИЧНЫМ. Сейчас `_mupdf_perpage` зовется только при
   `sum(per) < TEXT_MIN` НА ВЕСЬ ФАЙЛ, и случай cd3582b6c4 мимо: сумма 501 больше порога, а
   четыре страницы текста ослепли (PDFium 0/0/0/1 против MuPDF 82/344/547/174). Теперь: если
   на СТРАНИЦЕ pypdfium2 читает меньше, чем MuPDF, текст этой страницы берется у MuPDF.
   Значит текстовый слой не теряется тоже.

   Обе половины вместе дают: ФАКТУРА НЕ ТЕРЯЕТСЯ НИ В ОДНУ СТОРОНУ. Лишний OCR допустим -
   Apple Vision локальный и бесплатный, тратится время, а не деньги и не фактура.

3) ПОРОГ TEXT_MIN = 40 НЕ ДВИГАТЬ. Решение владельца: порог тут ни при чем.

4) КАЖДЫЙ ПОСТРАНИЧНЫЙ ЗАПАСНОЙ ПУТЬ ПЕЧАТАЕТСЯ ВСЛУХ с идентификатором материала, номером
   страницы и причиной. Молчаливый запасной путь неотличим от отсутствия основного.

5) ПЕРЕСОБРАТЬ ОТЧЕТ ПО КОРПУСУ под новый код, тем же прибором и с той же базой сравнения.

ЛИЦЕНЗИЯ: постраничный MuPDF - это БОЛЬШЕ обращений к AGPL-пакету, а не меньше. Блокер
остается отложенным по решению владельца 02.09.2026 и обязан всплыть в приемке очереди
публикации. Записать это в шапке прибора там же, где уже стоит решение.

ПЯТЫЙ КРУГ. КОД УЖЕ ВЕРЕН - ОСТАЛСЯ ТОЛЬКО ОТЧЕТ. Не переписывай прибор заново.

Круг 4 дал красный, и снова по делу, но дефект СУЗИЛСЯ до одного: отчет опять снят раньше кода.

    17:29:59  .helioz/state/plan/M09-korpus-otchet.md   <- отчет
    17:42:33  scripts/markdown_extract.py               <- постраничный путь дописан ПОСЛЕ

РЕШЕНИЕ ВЛАДЕЛЬЦА В КОДЕ УЖЕ ЕСТЬ И РАБОТАЕТ - это показала точечная проба проверяющего на
шипнутом коде:

    cc189caefb  dec: [13, 13, 25, 38, 0]              -> сторона scan (альбом ОСТАЛСЯ сканом)
    cd3582b6c4  mupdf_text_pages: [0, 1, 2, 3, 5]     -> текст ослепших страниц взят у MuPDF

Осторожное правило держит, постраничный запасной путь работает, потери нет ни в одну сторону.

ЧЕГО НЕ ХВАТАЕТ - ТОЛЬКО УЛИКИ:
1) Прогон по корпусу ОБОРВАН на 450 из 650. Сырой файл `.helioz/state/plan/_m09_progon.json`
   весит 99 байт и содержит лишь `warning: The 'fitz' API is deprecated`.
2) Отчет описывает ЦЕЛОФАЙЛОВЫЙ запасной путь и цитирует строку stderr «на весь файл», которой
   в коде больше нет (живой код печатает «материал <id>, стр. N»).
3) Вердикт отчета «ПОРОГ ПЕРЕЙДЕН, фото теряется» - это утверждение О ПОТЕРЕ, прямо обратное
   тому, что теперь требует пункт (б).

СДЕЛАЙ РОВНО ЭТО И БОЛЬШЕ НИЧЕГО:
  - догони прогон по ВСЕМ 650 материалам шипнутым кодом, до конца, без обрыва;
  - сохрани сырую улику полностью (json прогона не должен быть в 99 байт);
  - пересобери отчет ПОД НОВЫЙ КОД и докажи в нем ОТСУТСТВИЕ ПОТЕРИ В ОБЕ СТОРОНЫ: множество
    сканов только расширяется против старой базы MuPDF, и ни один текстовый слой не потерян;
  - обезличивание идентификатором - прибором, и в блоках stderr тоже.

ЗАПРЕТ: не трогать логику `markdown_extract.py` и `render_tail.py` - она принята. Не сокращать
корпус. Не прикладывать отчет, снятый раньше кода: это третий раз подряд, и каждый раз он стоит
полного круга приемки.

## Промт проверяющего
ВЕРДИКТ: зеленый|красный
Отчет исполнителя не читай. Твоя песочница ТОЛЬКО ДЛЯ ЧТЕНИЯ: временный каталог тебе
не создать. check_cmd и probe_cmd гоняет ГЕЙТ, а не ты - враждебная проба строит сломанный
вход и потому принадлежит гейту. Не пытайся их запускать: красный на «mktemp: Operation not
permitted» будет ложным, а от ложных красных судью перестают читать.
Проверяй утверждения ниже своими средствами: чтением кода, грепом, запуском того, что
записи не требует. Сомнение трактуй в красную сторону. При красном назови точный путь и условие.

Отдельно проверь: (а) основной путь - pypdfium2, а MuPDF присутствует ТОЛЬКО как запасной и только там,
    где основной читает около нуля; (б) отчет по корпусу пересобран под НОВЫЙ код и доказывает ОТСУТСТВИЕ ПОТЕРИ В ОБЕ
    СТОРОНЫ: ни одна страница не потеряла OCR против старой базы MuPDF (множество сканов
    только расширяется) и ни один текстовый слой не потерян (там, где PDFium читает меньше
    MuPDF, текст взят у MuPDF). Прежнее «ни один материал не перешел порог» ОТМЕНЕНО
    решением владельца 02.09.2026 - требовать его теперь значит требовать вранья; (в) интерфейс приборов не изменился -
вызывающие работают; (г) на настоящем PDF из _templates текст извлекается и не пуст;
(д) уход на запасной путь ПЕЧАТАЕТСЯ в stderr с именем файла и причиной - проверь запуском;
(е) порог TEXT_MIN не изменен ни на единицу - сверь с git diff;
(ж) в шапке прибора записано, что MuPDF - отложенный лицензионный блокер по решению
    владельца 02.09.2026, а не снятый.
