---
id: M01
title: Ворота протокола: шаг не стартует без отметки предыдущего
goal: "Порядок шагов конвейера держит код: агент шага не запускается без отметки предыдущего шага на диске; спавн охотника заблокирован без маркера готовности карты и без свежего preflight с нулевым кодом; правило, невыразимое хуком, удалено из текста."
paths:
  - scripts/claude_guard.py
  - .claude/settings.json
  - .claude/workflows/themiz-pipeline.js
requires: [R05]
executor: kimi
verifier: claude
check_cmd: "python3 scripts/claude_guard.py --selftest && node --check .claude/workflows/themiz-pipeline.js"
probe_cmd: "d=$(mktemp -d) && git ls-files -z --cached --others --exclude-standard | tar --null -T - -cf - | tar -xf - -C \"$d\" && rm -f \"$d/scripts/claude_guard.py\" && ! (cd \"$d\" && HELIOZ_HOME=\"$d\" THEMIZ_HOME=\"$d\" sh -c \"$CHECK\")"
stop: "Если отличить спавн агента шага от служебного спавна нельзя детерминированно - стоп, к владельцу. Блокировать все спавны подряд запрещено: это остановит конвейер, а не починит его."
price: "2-3 часа, 250-350k токенов"
kind: default
---
## Промт исполнителя
КОРЕНЬ НОМЕР ОДИН РАЗБОРА, и он породил 32 ошибки из 94: ворота - совет, а не запрет. Правила
протокола живут текстом в CLAUDE.md и в скиллах, а текст исполняется вероятностно.

Доказательства из боевого прогона 01.09.2026, все числа из журнала:
  - проводник themiz-pipeline не вызван НИ РАЗУ: ноль обращений к Skill и Workflow в основном
    потоке и во всех 54 транскриптах. Все 27 спавнов агентов сделаны напрямую;
  - preflight в 16:40 напечатал «источник sudact.ru НЕ отвечает» и вернул НОЛЬ - после чего рой
    сделал 271 сетевой вызов и сжег 62,6 млн токенов (треть прогона, около 119 долларов);
  - запрет «второй раз doc-reviewer не звать» нарушен дважды САМИМ ОРКЕСТРАТОРОМ, 23,19 доллара;
  - маркер «ФАКТУРА ЗАМОРОЖЕНА» поставлен в 16:32, после чего фактура правилась минимум четырежды,
    причем новое требование вставлено ПЕРЕД маркером;
  - шаг Б выполнен двадцать первым действием, шаг 6 не выполнен вовсе.
Контраст прямой: там, где ворота реализованы хуком, они сработали - сборка чужим конвертером
в папку готовых заблокирована, запись артефактов вне порядка отбита. Детерминированный слой держит.

ПОРЯДОК ИЗМЕНЕН 02.09.2026. Прежде задача ждала D02 (жесткий слой permissions.deny). D02
припаркована: ее селфтест честно краснеет на окружении — установленный Claude Code 2.1.207 младше
2.1.228, а до этой версии Edit(path) в permissions.deny не гарантирует блокировку Write, то есть
жесткий внешний слой на этой машине не жесткий. Решение о том, обновлять ли CLI, принимает владелец.

Связь между задачами была не смысловая, а по общим файлам, и это гейт стережет сам на старте.
Работай без D02. Ничего из ее части НЕ делай: permissions.deny не трогай, это ее предмет.

СДЕЛАЙ ТРИ ВЕЩИ, ВСЕ АДДИТИВНО.

1) РАСШИРЬ МАТЧЕР PreToolUse до Read|Write|Edit|NotebookEdit|Bash|Agent|WebFetch|WebSearch.
   Ни один старый инструмент не снимать. Учти: рядом уже стоит ОТДЕЛЬНЫЙ PreToolUse с матчером
   Agent, занятый хуком Entire. Два хука на одном матчере уживаются - реши и запиши в коде, что
   происходит при нашем коде 2, и не сломай отслеживание Entire: отключать его нельзя.

2) ОТМЕТКА ШАГА НА ДИСКЕ. Заведи машинное состояние прогона (файл прогона под делом, имя и место
   согласуй через scripts/case_paths.py, второго адреса не заводи). Правила:
   - агент шага N не запускается, если на диске нет отметки шага N-1;
   - спавн practice-hunter-* блокируется без маркера «КАРТА ГОТОВА» в карте знаний дела.
     ИСКЛЮЧЕНИЙ НЕТ НИ ОДНОГО. Решение владельца 01.09.2026 прямое: «поиск практики бессмысленен,
     пока нет карты дела». Жесткий срок основанием НЕ является. Единственное исключение владельца -
     взять практику из НАЗВАННОГО другого дела, и в этой ветке охотники не запускаются вовсе,
     поэтому гейту исключение не нужно и городить его ЗАПРЕЩЕНО: исключения, которого нет в коде,
     невозможно обойти;
   - спавн охотника блокируется, если последний preflight_search вернул ненулевой код и владелец
     не дал явного решения записью на диске;
   - Agent(doc-reviewer) разрешен только из doc-drafter, не из основного потока.
   Дело выводи из пути в промпте либо из активного лока. Дела определить нельзя - веди себя как
   раньше: fail-open только на НЕОПОЗНАННОМ деле, никогда на опознанном.

3) ПРОВОДНИК ОБЯЗАТЕЛЕН. Отметка запуска проводника ложится в файл прогона; спавн ключевых агентов
   дела без нее блокируется с текстом, называющим проводник. Ворота внутри самого файла проводника
   не ставь: за прогон его не открыли ни разу, и правило там - это правило в месте, куда исполнение
   не заходит.

САМОЕ ВАЖНОЕ - ЧЕТВЕРТОЕ. Селфтест обязан ловить рассогласование: читать .claude/settings.json,
собирать множество инструментов из матчера и сверять с множеством, которое сторож реально
обрабатывает в main(). Расхождение - красный код с перечнем. Сегодня селфтест зеленый на
отключенном стороже, и это и есть фикстура по эту сторону порога.

НЕ ТРОГАЙ содержательную юридическую логику и не ослабляй ни одной существующей блокировки.

## Промт проверяющего
ВЕРДИКТ: зеленый|красный
Отчет исполнителя не читай. Твоя песочница ТОЛЬКО ДЛЯ ЧТЕНИЯ: временный каталог тебе
не создать. check_cmd и probe_cmd гоняет ГЕЙТ, а не ты - враждебная проба строит сломанный
вход и потому принадлежит гейту. Не пытайся их запускать: красный на «mktemp: Operation not
permitted» будет ложным, а от ложных красных судью перестают читать.
Проверяй утверждения ниже своими средствами: чтением кода, грепом, запуском того, что
записи не требует. Сомнение трактуй в красную сторону. При красном назови точный путь и условие.

Отдельно проверь:
(а) матчер содержит Agent, WebFetch, WebSearch и НЕ ПОТЕРЯЛ ни одного старого инструмента;
(б) хуки Entire на месте и работают;
(в) спавн охотника без маркера карты дает ненулевой код; с маркером - нулевой;
(г) в стороже НЕТ ни одной ветки, пропускающей охотника без маркера карты - ни по сроку,
    ни по санкции, ни по переменной окружения. Нашел такую ветку - красный;
(д) Agent(doc-reviewer) из основного потока блокируется, из doc-drafter проходит;
(е) селфтест краснеет, если временно убрать Agent из матчера (делай в копии);
(ж) прогони весь селфтест и сверь число проверок с тем, что было до задачи: ни одна не потеряна.
