---
id: M14
title: Скилл составителя ссылается на единый источник, а не повторяет его
goal: "Дом готового документа и число раундов рецензии в скилле составителя берутся из case_paths.py ссылкой, а не повторяются числом и путем; машинная проверка одного дома зеленеет."
paths:
  - .claude/skills/doc-drafter/SKILL.md
requires: [M12, M13]
executor: kimi
verifier: claude
check_cmd: "python3 scripts/case_paths.py --selftest"
probe_cmd: "d=$(mktemp -d) && git ls-files -z --cached --others --exclude-standard | tar --null -T - -cf - | tar -xf - -C \"$d\" && printf '%s\\n' 'До 5 раундов' >> \"$d/.claude/skills/doc-drafter/SKILL.md\" && ! (cd \"$d\" && HELIOZ_HOME=\"$d\" THEMIZ_HOME=\"$d\" sh -c \"$CHECK\")"
stop: "Если убрать число раундов из текста нельзя, не сделав скилл непонятным исполнителю - стоп, к владельцу: читаемость промта для агента важнее формальной чистоты."
price: "40-60 мин, 60-90k токенов"
kind: default
---
## Промт исполнителя
Задача M12 завела машинную проверку «один дом документа». Она немедленно нашла четыре нарушения
в этом файле, и все четыре настоящие:

    .claude/skills/doc-drafter/SKILL.md:203  готовый файл назван в служебной папке
    .claude/skills/doc-drafter/SKILL.md:122  число раундов 2 повторено вместо ссылки на case_paths.py
    .claude/skills/doc-drafter/SKILL.md:128  то же
    .claude/skills/doc-drafter/SKILL.md:190  то же

ЗАЧЕМ ЭТО ВАЖНО. Главный системный дефект прогона 01.09.2026: AGENTS.md и команда черновика
предписывали одно место готового документа, а этот скилл - другое. Конституция читается первой,
агент исполнил ее буквально и был ПРАВ, а документ ушел не туда. Повторенная конвенция расходится
всегда; вопрос только когда.

Про число раундов то же самое: лимит уже живет в конфиге с причиной и датой истечения (задача M02),
а здесь он повторен числом три раза. Разойдется на первом же изменении лимита.

ПРАВКА ЭТОГО ФАЙЛА РАЗРЕШЕНА. Прошлый круг ты остановился, спросив добро на запись в
.claude/skills/doc-drafter/SKILL.md, и предложил ровно четыре верные правки. Разрешение дано:
файл в paths задачи, сторож Фемиды его пропускает (проверено: Write в него дает код 0),
в permissions.deny его нет. Делай правки и не спрашивай.

СДЕЛАЙ:
1) Строка 203: дом готового документа назови ССЫЛКОЙ на scripts/case_paths.py, а не путем в тексте.
2) Строки 122, 128, 190: число раундов замени ссылкой на источник. Исполнителю скилла должно
   остаться понятно, ГДЕ узнать актуальное значение - назови команду, которой он его получит.
3) Проверь грепом, что в файле не осталось ДРУГИХ повторов конвенции: путей дома документа,
   числа раундов, имен служебных папок. Проверка M12 их назовет - гони ее до сдачи.
4) Смысл скилла не менять: это рабочая инструкция составителю судебных документов, она должна
   остаться исполнимой и понятной.

НЕ ТРОГАЙ шаг сдачи с вызовами через gate.sh - его поставили задачи R07 и M13.

## Промт проверяющего
ВЕРДИКТ: зеленый|красный
Отчет исполнителя не читай. Твоя песочница ТОЛЬКО ДЛЯ ЧТЕНИЯ. check_cmd и probe_cmd гоняет ГЕЙТ.

Проверяй чтением и грепом:
(а) машинная проверка одного дома больше не называет этот файл;
(б) число раундов в тексте не повторено - есть ссылка на источник и способ его узнать;
(в) вызовы шага сдачи через gate.sh на месте и не изменены;
(г) скилл остался исполнимой инструкцией: составитель по нему поймет, куда класть готовый
    документ и сколько раундов допустимо;
(д) ни один шаг скилла не удален.
