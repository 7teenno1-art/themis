---
id: M07
title: Каналы и квоты: белый список правит прибор, а не рука
goal: "Состояние каналов и квот - общий файл с временем жизни, который читают все агенты; правка белого списка идет только через прибор; мертвый источник не опрашивается сто двадцать восемь раз за прогон."
paths:
  - scripts/channel_grant.py
  - knowledge/allowed-services.md
  - scripts/practice_search.py
requires: [M01]
executor: kimi
verifier: claude
check_cmd: "python3 scripts/channel_grant.py --selftest && python3 scripts/practice_search.py --selftest"
probe_cmd: "d=$(mktemp -d) && git ls-files -z --cached --others --exclude-standard | tar --null -T - -cf - | tar -xf - -C \"$d\" && rm -f \"$d/scripts/channel_grant.py\" && ! (cd \"$d\" && HELIOZ_HOME=\"$d\" THEMIZ_HOME=\"$d\" sh -c \"$CHECK\")"
stop: "Если запрет прямой правки белого списка закроет владельцу возможность решить вопрос быстро - стоп, к владельцу: его санкция обязана иметь машинный путь, а не обход."
price: "2-2,5 часа, 200-280k токенов"
kind: default
---
## Промт исполнителя
Прогон 01.09.2026: preflight в 16:40 честно сказал «источник sudact.ru НЕ отвечает» - после чего
рой сделал 271 сетевой вызов и сжег 62,6 млн токенов, треть прогона, около 119 долларов. Мертвый
источник опрашивался 128 раз. Белый список внешних сервисов был дописан ПОСРЕДИ ПРОГОНА
проверяемой стороной - прямо сейчас эта правка висит в дереве незакоммиченной.

СДЕЛАЙ:
1) ОБЩИЙ ФАЙЛ СОСТОЯНИЯ КАНАЛОВ И КВОТ на прогон, со временем жизни записи. Его читают ВСЕ агенты:
   сегодня квота поиска - догадка каждого охотника по отдельности, а должна быть общим счетом.
   Мертвый канал, помеченный в этом файле, не опрашивается повторно до истечения времени жизни.
2) channel_grant.py - ЕДИНСТВЕННЫЙ законный путь правки knowledge/allowed-services.md. Прямая
   правка файла запрещается жестким слоем. Санкция владельца проходит через прибор и становится
   машинной записью с датой и причиной, а не абзацем, дописанным по ходу.
3) practice_search: полнота выдачи. Прибор УЖЕ умеет --partition, --per-region, --max-regions,
   несет CAP_HINT = 500 и partition_exit_code, краснеющий на неполном охвате. Дефект не в приборе,
   а в том, что его не звали: за прогон Линкей ходил ТОЛЬКО в режиме url, 54 раза из 54, ни разу
   с обходом, кешем или параллельностью. Твоя часть: убедиться, что --partition не теряет --area
   и --case-doc, что остановка идет по двум пустым страницам подряд, и что недобор отличается от
   упора в потолок источника.
4) Вежливость к источнику важнее скорости: параллельность поднимай до объявленной константы с
   комментарием, откуда взято число, а не до отказа источника.

РЕШЕНИЕ ВЛАДЕЛЬЦА о robots.txt источника от 03.08.2026 живет константой SUDACT_SEARCH_ALLOWED.
НЕ ТРОГАЙ его и не заводи второй точки правды.

ВТОРОЙ КРУГ, ЗАМЕР 02.09.2026 17:10. Прежний исполнитель остановился, спросив владельца:
коммитить ли правку `knowledge/allowed-services.md` от 01.09.2026 как есть или переоформить
прибором. ФОРМА этого вопроса владельцу не принадлежит - ее уже решает цель самой задачи
(«белый список правит прибор, а не рука»), а СУТЬ владелец решил 01.09.2026. Замеры:

    python3 scripts/channel_grant.py --list   -> ПУСТО, ни одной машинной записи
    channel_grant.py:3   «ЕДИНСТВЕННЫЙ законный путь правки knowledge/allowed-services.md»
    channel_grant.py:34  REGISTRY = knowledge/allowed-services.md

То есть висящая правка сделана РУКОЙ ровно в тот файл, который по объявлению прибора рукой
не правится, и машинный раздел «Машинные записи каналов» пуст. Это не спор о вкусе, это
нарушение контракта, который задача и заводит.

ДВЕ СТРОКИ ПРАВКИ РАЗНЫЕ, И ЛЕЧАТСЯ ПО-РАЗНОМУ.

1) ПЛОЩАДКИ ОБЪЯВЛЕНИЙ (avito.ru, cian.ru, domclick.ru, realty.yandex.ru). Оформляются
   прибором: четыре записи `--host ... --reason ... --owner-approved --sanction «решение
   владельца 01.09.2026»`. Прозаическая строка таблицы при этом ОСТАЕТСЯ: в ней оговорки,
   которых машинная запись не несет, - что наружу уходит только адрес дома и площадь, без
   ФИО и номера квартиры, что массовый обход и обход антибот-защиты не строится, и что
   собранное обосновывает ходатайство об экспертизе, а НЕ доказывает рыночную цену.
   ЭТО ПРАВОВАЯ СТРАХОВКА, А НЕ УКРАШЕНИЕ. Стереть ее при переоформлении ЗАПРЕЩЕНО.

2) СУДАКТ ЧЕРЕЗ ЛИНКЕЯ. Прибор эту запись принять НЕ МОЖЕТ и не должен:

    channel_grant.py:46  TRUTH_ELSEWHERE = {"sudact.ru": ... константа
                         SUDACT_SEARCH_ALLOWED в scripts/practice_search.py ...}

   Прибор отказывается заводить вторую точку правды по этому хосту сознательно. Но новая
   строка описывает ДРУГОЙ путь доступа к тому же хосту (Линкей по известному URL против
   асинхронного поиска practice_search). Один хост, два пути, одна объявленная точка правды -
   это ровно корень номер два разбора.

   ЗДЕСЬ STOP К ВЛАДЕЛЬЦУ, и вопрос ставится так: точка правды по sudact.ru остается ОДНА
   (константа в practice_search.py) и путь Линкея подчиняется ей же, либо точек становится
   две и тогда прибор обязан знать про обе. Не выбирать молча.


РЕШЕНИЕ ВЛАДЕЛЬЦА 02.09.2026, STOP ЗАКРЫТ. Дословно: «Точек две, прибор знает про обе».

ПОЧЕМУ ТАК, А НЕ ОДНОЙ ТОЧКОЙ. Пути к хосту два, и запрет источника накрывает только один:

  - АСИНХРОННЫЙ ПОИСК (`practice_search.py`) идет через `/{раздел}/doc_ajax/`, а его
    `robots.txt` закрывает для `User-agent: *`. Риск принят владельцем 03.08.2026.
  - ОТКРЫТИЕ КОНКРЕТНОГО АКТА ПО ИЗВЕСТНОМУ URL (Линкей, `/{раздел}/doc/<id>/`) под запрет
    источника НЕ ПОДПАДАЛО НИКОГДА - это записано и в самом practice_search.py:334-335.

Подчинить второй путь выключателю первого значило бы гасить то, чего источник не запрещал:
`THEMIZ_SUDACT_SEARCH=0` убивал бы и законное чтение акта по ссылке. Поэтому точки две.

СДЕЛАЙ:

1) `SUDACT_SEARCH_ALLOWED` НЕ ТРОГАТЬ НИ НА ЗНАК. Это по-прежнему единственная точка правды
   ПОИСКА, и проверяющий сверяет это отдельным пунктом (е).

2) `TRUTH_ELSEWHERE` ПЕРЕПИСАТЬ, А НЕ СТЕРЕТЬ. Сейчас он отбивает хост `sudact.ru` целиком.
   Должен отбивать РОВНО ПОИСК и пропускать путь по известному URL. То есть ключ отказа
   перестает быть «хост» и становится «хост + путь доступа»: запись о поиске по-прежнему
   заводить нельзя (ее точка правды - константа), запись о пути Линкея завести МОЖНО.

3) МАШИННАЯ ЗАПИСЬ ЛИНКЕЯ ОБЯЗАНА НАЗЫВАТЬ ВТОРУЮ ТОЧКУ. Две точки правды расходятся молча -
   это главный риск выбранного владельцем варианта, и лечится он только перекрестной ссылкой:
   в записи о Линкее прибор печатает, что ПОИСК живет константой `SUDACT_SEARCH_ALLOWED` и
   этой записью НЕ управляется. Читающий любую из двух точек видит вторую.

4) СЕЛФТЕСТ ЗАКРЕПЛЯЕТ ОБА УТВЕРЖДЕНИЯ, иначе они разъедутся первой же правкой:
   - попытка завести запись о ПОИСКЕ по sudact.ru отбивается с указанием на константу;
   - запись о пути по известному URL заводится и несет перекрестную ссылку на константу.

5) ПЛОЩАДКИ ОБЪЯВЛЕНИЙ - четыре машинные записи прибором (`--host ... --reason ...
   --owner-approved`), как и было предписано. Прозаические оговорки о периметре данных и
   доказательственной силе ОСТАЮТСЯ в таблице: это правовая страховка, стирать запрещено.

6) СТОРОЖ ПД ДЕРЖИТ ЭТУ ПРАВКУ. `python3 scripts/pd_guard.py --tree` дает по строке 64
   `knowledge/allowed-services.md` находку «публикационная утечка (домашний путь проекта)»:
   в тексте назван домашний путь Линкея. Репозиторий публичный. Убрать путь из прозы -
   инструмент называется именем, а не адресом на диске машины владельца. Гейт ПД не обходить.

КРУГ 02.09.2026 22:54, КРАСНЫЙ ПО ДЕЛУ. Три дефекта, все с путем и строкой.

ЗАЧТЕНО И НЕ ПЕРЕДЕЛЫВАТЬ. Решение владельца про ДВЕ ТОЧКИ ПРАВДЫ выполнено:
прямая правка allowed-services.md отбивается хуком (Write/echo/sed -i -> код 2),
channel_grant --list печатает 5 машинных записей с датой, причиной и санкцией,
запись по sudact несет перекрестную ссылку на SUDACT_SEARCH_ALLOWED, прозаические
оговорки о периметре ПД и доказательственной силе целы, находки pd_guard по файлу
больше нет. Также зелены (г) --partition держит --area и --case-doc и (е) константа
не тронута. Это все не трогать.

ДЕФЕКТ 1, пункт (д). НЕДОБОР И УПОР В ПОТОЛОК ДАЮТ ОДИН И ТОТ ЖЕ КОД.

    practice_search.py:404  RC_COVERAGE_INCOMPLETE = 5 - единственная константа кода
    practice_search.py:407  partition_exit_code -> 5, если есть report["error"]
    practice_search.py:717-724  недобор и упор падают в ОДИН список thin
    grep -n "^RC_" practice_search.py -> одна строка

Различие живет прозой в поле «причина» отчета, а пункт требует РАЗНЫХ КОДОВ
ВОЗВРАТА. Вызывающий скрипт читает код, а не прозу. Заведи вторую константу и
разведи два случая по разным кодам; проза остается как была.

ДЕФЕКТ 2, пункт (в). ОБЩЕГО СЧЕТА КВОТЫ НЕТ - ПРИБОР ЕСТЬ, СЧЕТА НЕТ.

    scripts/channels.py:123  spend() НЕ ЗОВЕТ НИКТО
    grep -rn "channels\." scripts .claude docs -> только status/mark/is_dead
    scripts/preflight_search.py:230 после правки не тронута и по-прежнему гласит:
      ("WebSearch (квота сессии)", None, "программно не проверяется",
       "лимит 200 запросов на сессию - спросить у охотника в первом отчете")

Это ровно та догадка отдельного охотника, которую задача снимает. Расход обязан
списываться в общий счет там, где он фактически происходит, и строка preflight
обязана читать общий счет, а не советовать спросить.

ДЕФЕКТ 3, пункт (б). ОТКАЗ ПО МЕРТВОМУ КАНАЛУ ВИСИТ НА ПЕРЕМЕННОЙ, КОТОРУЮ НИКТО
НЕ СТАВИТ.

    practice_search.py:365-367 и preflight_search.py:211 читают $THEMIZ_CASE
    grep -rn "THEMIZ_CASE" scripts .claude docs -> ТОЛЬКО читатели и селфтесты
    у practice_search.py флага --case нет: argparse знает только --case-doc (стр. 1203)

В боевом прогоне без переменной dead_channel_note() возвращает пустую строку, и
мертвый источник опрашивается снова - то есть дефект, ради которого задача
заведена (128 опросов мертвого источника), в бою остается живым. Дай прибору
штатный путь узнать дело: флаг либо вывод из пути, и чтобы селфтест проверял
поведение БЕЗ переменной окружения.

ОБЩЕЕ ПРАВИЛО КРУГА: не чинить прозой то, что пункт требует кодом. Проверяющий
читает код возврата, счетчик и поведение без переменной, а не текст отчета.

## Промт проверяющего
ВЕРДИКТ: зеленый|красный
Отчет исполнителя не читай. Твоя песочница ТОЛЬКО ДЛЯ ЧТЕНИЯ: временный каталог тебе
не создать. check_cmd и probe_cmd гоняет ГЕЙТ, а не ты - враждебная проба строит сломанный
вход и потому принадлежит гейту. Не пытайся их запускать: красный на «mktemp: Operation not
permitted» будет ложным, а от ложных красных судью перестают читать.
Проверяй утверждения ниже своими средствами: чтением кода, грепом, запуском того, что
записи не требует. Сомнение трактуй в красную сторону. При красном назови точный путь и условие.

Отдельно проверь: (а) прямая правка allowed-services.md отбивается, а правка через прибор проходит
и оставляет дату с причиной; (б) канал, помеченный мертвым, не опрашивается повторно; (в) квота
поиска - общий счет, а не счет одного агента; (г) --partition не теряет --area и --case-doc;
(д) недобор и упор в потолок различаются кодом возврата; (е) SUDACT_SEARCH_ALLOWED не тронут.
