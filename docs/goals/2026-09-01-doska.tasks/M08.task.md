---
id: M08
title: Контекст оркестратора перестает переоплачиваться
goal: "Расход по статье повторной доставки контекста виден отдельной колонкой; у оркестратора есть потолок контекста и обязательная разгрузка между фазами; стартовый контекст разобран по составу."
paths:
  - scripts/token_ledger.py
  - .claude/CLAUDE.md
requires: [M03]
executor: kimi
verifier: claude
check_cmd: "python3 scripts/token_ledger.py --selftest && test $(wc -l < .claude/CLAUDE.md) -le 200 && test $(wc -c < .claude/CLAUDE.md) -le 20480"
probe_cmd: "d=$(mktemp -d) && git ls-files -z --cached --others --exclude-standard | tar --null -T - -cf - | tar -xf - -C \"$d\" && awk 'BEGIN{for(i=0;i<1000;i++)print \"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"}' >> \"$d/.claude/CLAUDE.md\" && ! (cd \"$d\" && HELIOZ_HOME=\"$d\" THEMIZ_HOME=\"$d\" sh -c \"$CHECK\")"
stop: "Если сокращение CLAUDE.md уносит правило, которое НЕ выражается хуком - стоп, к владельцу: такое правило остается текстом, и это записывается причиной."
price: "2-3 часа, 200-300k токенов"
kind: default
---
## Промт исполнителя
САМАЯ ДОРОГАЯ СТАТЬЯ ПРОГОНА, и ее никто не назвал задачей. Из 360,17 доллара:
  - 200,55 доллара (55,7 процента) - повторная доставка УЖЕ ПРОЧИТАННОГО контекста Opus.
    Контекст оркестратора рос с 96 480 до 503 962 токенов без единого сброса и переоплачивался
    на каждом из 189 вызовов. 104 запроса из 189 шли с контекстом больше 300 000;
  - еще 34,6 процента трафика - переподача системных преамбул;
  - стартовый контекст 95 792 токена на КАЖДОМ из 227 запросов. Его источник на диске:
    .claude/CLAUDE.md - 45 181 байт при 199 строках, плюс hunters-common.md на 16,7 КБ.

СДЕЛАЙ:
1) token_ledger получает колонку «преамбула на число вызовов»: сколько заплачено за повторную
   доставку одного и того же. Цифра из журнала, не оценка. Плюс потолок контекста фазы: превышение
   - ненулевой код, а не молчание.
2) РАЗБЕРИ СОСТАВ .claude/CLAUDE.md. Каждое правило раздели на два вида: то, что ВЫРАЗИМО хуком,
   и то, что нет. Выразимое уходит в машину - и тем самым уходит из стартового контекста каждого
   запроса. Невыразимое остается текстом, и для каждого такого правила запиши ПРИЧИНУ, почему оно
   не машинизируемо. Правило разбора дословно: «правило, которое не может быть выражено хуком,
   должно быть удалено из CLAUDE.md, а не оставлено как пожелание».
3) Лимит 200 строк на CLAUDE.md - неприкосновенен, он уже есть. Но 199 строк при 45 КБ значит,
   что строки длинные: меряй БАЙТЫ, а не строки, и назови достигнутую экономию замером.
4) Обязательная разгрузка контекста оркестратора между фазами: запиши механизм, а не пожелание.

НЕ УДАЛЯЙ ни одного правила, не переселив его в машину или не записав причину.

ВТОРОЙ КРУГ, ЗАМЕР 02.09.2026 17:20. ПРОВЕРКА ЗАДАЧИ МЕРИЛА НЕ ТУ ВЕЛИЧИНУ И БЫЛА ЗЕЛЕНОЙ
ПРИ НЕТРОНУТОМ ДЕФЕКТЕ. Стояло `test $(wc -l < .claude/CLAUDE.md) -le 200`. Замер сегодня:

    .claude/CLAUDE.md          200 строк    46138 байт
    .claude/hunters-common.md   95 строк    16711 байт

Ровно 200 строк - предел взят впритык, а байт стало БОЛЬШЕ, чем названо дефектом в этой же
задаче (45181). Строка растягивается свободно, и предел по строкам сокращения не требует.
Задача сама говорит: МЕРИТЬ БАЙТЫ, НЕ СТРОКИ, - а проверка мерила строки.

Это корень номер один разбора в чистом виде: правило записано, исполнение до него не доходит,
и прибор при этом зеленый. Тот же класс, что и «селфтест согласился сам с собой» в M16.

ПРОВЕРКА ИСПРАВЛЕНА: держатся ОБА предела - 200 строк (отдельная конвенция проекта) И 20480
байт. Порог байтов - это ПРИЕМОЧНАЯ ПЛАНКА задачи: сокращение более чем вдвое от замеренных
46138. Проба теперь валит именно байтовый предел, а не строчный.

ЕСЛИ ПЛАНКА НЕ БЕРЕТСЯ без потери правила, которое хуком не выражается, - работает пункт stop
этой задачи: СТОП к владельцу, правило остается текстом с записанной причиной. Подгонять порог
под то, что получилось, ЗАПРЕЩЕНО: так дефект и дожил до сегодня.

## Промт проверяющего
ВЕРДИКТ: зеленый|красный
Отчет исполнителя не читай. Твоя песочница ТОЛЬКО ДЛЯ ЧТЕНИЯ: временный каталог тебе
не создать. check_cmd и probe_cmd гоняет ГЕЙТ, а не ты - враждебная проба строит сломанный
вход и потому принадлежит гейту. Не пытайся их запускать: красный на «mktemp: Operation not
permitted» будет ложным, а от ложных красных судью перестают читать.
Проверяй утверждения ниже своими средствами: чтением кода, грепом, запуском того, что
записи не требует. Сомнение трактуй в красную сторону. При красном назови точный путь и условие.

Отдельно проверь: (а) колонка повторной доставки считается по журналу и сходится с итогом;
(б) превышение потолка контекста дает ненулевой код; (в) каждое удаленное из CLAUDE.md правило
либо стоит хуком, либо имеет записанную причину - сверь список до и после ПОШТУЧНО;
(г) файл остался в пределах 200 строк; (д) экономия названа замером в байтах, а не обещанием.
