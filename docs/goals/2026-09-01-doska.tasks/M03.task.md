---
id: M03
title: Контракт роя: тип, модель, бюджет, приемка, потолок
goal: "Лист роя не подменяется универсальным агентом; модель роли берется из политики, а не из заголовка агента; бюджет времени и токенов - аргумент спавна; приемка агента по маркеру на диске; одновременных агентов не больше шести."
paths:
  - scripts/claude_guard.py
  - scripts/model_policy.py
  - .claude/settings.json
requires: [M01]
executor: kimi
verifier: claude
check_cmd: "python3 scripts/claude_guard.py --selftest && python3 scripts/swarm_contract.py --selftest && python3 scripts/swarm_contract.py --audit-agents"
probe_cmd: "d=$(mktemp -d) && git ls-files -z --cached --others --exclude-standard | tar --null -T - -cf - | tar -xf - -C \"$d\" && rm -f \"$d/scripts/claude_guard.py\" && ! (cd \"$d\" && HELIOZ_HOME=\"$d\" THEMIZ_HOME=\"$d\" sh -c \"$CHECK\")"
stop: "Универсальный агент ВНЕ работы по делу запрещать нельзя: правило касается только задач дела. Если отличить одно от другого детерминированно нельзя - стоп, к владельцу."
price: "2-3 часа, 250-350k токенов"
kind: default
---
## Промт исполнителя
КОРЕНЬ НОМЕР ТРИ, породил 21 ошибку из 94: рой без контракта. Тип, модель, бюджет, приемка
и связь агента ничем не заданы.

Доказательства прогона:
  - 30 спавнов из 54 (55,6 процента) - general-purpose вне реестра ролей, на них 27,5 млн токенов;
  - 9 из 11 листьев охоты не позвали НИ ОДНОГО прибора Фемиды и сделали 156 веб-вызовов вслепую
    (14,06 млн токенов, 26,78 доллара);
  - три координатора собрали листовой рой тремя несовместимыми способами, один спавнил агентов
    собственного типа;
  - TaskStop не вызывался ни разу при ДЕВЯТИ агентах старше 20 минут;
  - агенты завершались, не выполнив задания: составителя будили четырьмя сообщениями, из 16
    сообщений восемь ушли на исправление одной вводной;
  - поправка владельца наследуется на один уровень: после указания пользоваться Линкеем
    координаторы перешли (обращения выросли с 10 до 29), а их листья продолжили веб-поиск еще
    51 раз - спавн листа не включает поправку в контракт;
  - харнесс пятнадцать раз молча подменил недоступную модель.

СДЕЛАЙ:
1) ТИПИЗИРОВАННЫЕ РОЛИ. Задача поиска практики, отданная универсальному агенту, - блок с
   называнием правильного типа листа. Спавн агентом агента собственного типа - блок.
2) МОДЕЛЬ ИЗ ПОЛИТИКИ. model_policy.py получает --audit-agents: сверка заголовков всех агентов
   в .claude/agents/ с таблицей политики. Расхождение - ненулевой код с перечнем. Заголовок
   агента перестает быть источником правды о модели.
3) ПОДМЕНА МОДЕЛИ ХАРНЕССОМ ПЕРЕСТАЕТ БЫТЬ НЕВИДИМОЙ. token_ledger считает событие подмены
   по реальной записи в журнале и выносит его отдельной строкой разреза, а не растворяет.
4) БЮДЖЕТ И ВРЕМЯ - АРГУМЕНТ СПАВНА. Лист получает потолок токенов и потолок времени. Сторожевой
   таймер на агента старше потолка: не убивать молча, а докладывать и предлагать остановку.
5) ПРИЕМКА АГЕНТА ПО МАРКЕРУ НА ДИСКЕ, не по его тексту. Агент, объявивший себя закончившим без
   маркера, считается незакрытым.
6) ПОТОЛОК ОДНОВРЕМЕННЫХ АГЕНТОВ - ШЕСТЬ. Число посчитано, а не объявлено: 8 логических ядер,
   потолок min(16, ядра минус 2). Рост числа листьев выше этого упирается в очередь операционной
   системы и ускорения не дает, а платится полная цена. Потолок держит код.
7) НАСЛЕДОВАНИЕ ПОПРАВКИ ВНИЗ ПО РОЮ. Спавн листа несет действующие поправки прогона; спавн без
   них при наличии действующей поправки - блок.

ВТОРОЙ КРУГ. Ты написал scripts/swarm_contract.py на 443 строки с настоящей начинкой -
потолок параллелизма, лимиты, приемка по маркеру, сверка моделей агентов. Работа хорошая, но
ПРИБОР-СИРОТА: греп по всему дому находит только его самого, ни один вызывающий его не знает.
Это ровно тот дефект, ради которого затеян весь ремонт - ворота написаны там, куда исполнение
не заходит. Такие ворота хуже отсутствующих: они создают ложное спокойствие.

Плюс проверка слоя защиты честно покраснела:

    ✗ permissions.deny не накрывает: scripts/swarm_contract.py
    ✗ sandbox.filesystem.denyWrite не накрывает: ./scripts/swarm_contract.py

Новые ворота обязаны быть защищены как ворота, иначе их правит кто угодно.

СДЕЛАЙ, НЕ ПЕРЕПИСЫВАЯ УЖЕ НАПИСАННОЕ:
1) ПОДКЛЮЧИ прибор. claude_guard на событии Agent обязан звать контракт роя и исполнять его
   вердикт. Проверяется грепом: вызывающий существует и не один только сам прибор.
2) ЗАРЕГИСТРИРУЙ его в защищаемом списке, чтобы permissions.deny и denyWrite его накрыли.
   Список собирается с диска - убедись, что новый прибор в него попадает по признаку, а не
   вписан руками.
3) Флаг --audit-agents оставь ТАМ, ГДЕ ОН ЖИВЕТ, в swarm_contract.py. Дублировать его в
   model_policy.py не надо: два места на одну работу - корень номер два.
4) Если после подключения окажется, что 443 строки нового прибора дублируют то, что уже умеет
   claude_guard, - скажи это прямо в отчете и предложи слияние. Не делай слияние молча.

ТРЕТИЙ КРУГ, ОДНА ПРАВКА. Прибор подключен, слой защиты его накрыл, селфтест сторожа
171/171 - все это сделано. Осталась ЛОЖНАЯ ТРЕВОГА в --audit-agents:

    расхождений frontmatter агентов с политикой: 2
      · council-role:  пин «sonnet» есть в политике, а файла агента нет
      · practice-leaf: пин «haiku»  есть в политике, а файла агента нет

Дом исправен, неправа проверка. В политике ДВА РАЗНЫХ ВИДА записей, и ты судишь их одинаково:

    "council-role":  {"L2": "sonnet", "L3": "sonnet"}     <- ЛОГИЧЕСКАЯ РОЛЬ
    "askacouncil":     "council-role"                     <- отображение скилла на роль
    "position-council": "council-role"

Логическая роль файла агента НЕ ИМЕЕТ и иметь не должна: на нее отображаются скиллы. Файловые
типы - это то, что лежит в .claude/agents/ (их 13: case-mapper, doc-drafter, doc-reviewer,
practice-hunter-classic и прочие).

СДЕЛАЙ:
1) Раздели два вида записей в аудите:
   - ФАЙЛОВЫЙ тип: файл в .claude/agents/ обязан существовать, и его frontmatter обязан
     совпадать с пином политики. Это то, что ты уже проверяешь;
   - ЛОГИЧЕСКАЯ роль: файла не ждем, но обязан существовать хотя бы ОДИН потребитель -
     отображение скилла или агента на эту роль. Роль без потребителя - мертвая запись, и вот
     она красная по праву.
2) Обратную сторону тоже проверь: файловый тип агента БЕЗ пина в политике - тоже расхождение.
   Сегодня 13 файлов, и каждый обязан быть назван политикой.
3) Ложная тревога хуже молчания: сторож, который краснеет на исправном доме, перестают читать.
   Это записано уроком прогона, не выдумка.

НЕ ЗАПРЕЩАЙ универсальный агент вне работы по делу.

КРУГ 02.09.2026 21:52, КРАСНЫЙ ПО ДЕЛУ. Дефект ОДИН, и он не про оформление.

ПОТОЛОК ШЕСТИ СУЩЕСТВУЕТ ТОЛЬКО КАК ПЕЧАТАЕМОЕ ЧИСЛО. Замеры судьи:

    scripts/swarm_contract.py:47  concurrency_cap() -> min(16, ядра-2)
    вызывающие: cmd_limits() (печать) и селфтест. БОЛЬШЕ НИКТО.
    scripts/claude_guard.py:2154 _agent_gate - ни одного счетчика живых агентов
    греп по дому на concurrency_cap вне swarm_contract.py -> ПУСТО

Седьмого одновременного агента отбивать НЕКОМУ. Пункт (в) требует, чтобы попытка
отбивалась, а на диске отбоя нет.

СЕЛФТЕСТ ЭТОГО НЕ ЛОВИТ И НЕ ПОЙМАЕТ: он сверяет concurrency_cap() с ТОЙ ЖЕ
ФОРМУЛОЙ max(1, min(16, cpu-2)). Тавтология. Зеленый селфтест отсутствие
принуждения не закрывает - это тот же урок, что и «проба, зеленая при обеих
подстановках».

СДЕЛАЙ:
1) СЧЕТ ЖИВЫХ АГЕНТОВ, а не печать числа. _agent_gate обязан знать, сколько
   агентов уже бежит, и отбивать того, кто выходит за потолок. Состояние счета -
   на диске (прогон переживает перезапуск процесса), снятие записи гарантировано
   при выходе агента, иначе счет протечет и заблокирует работу навсегда.
2) СЕЛФТЕСТ ПРОВЕРЯЕТ ПРИНУЖДЕНИЕ, А НЕ ФОРМУЛУ: завести N живых записей и
   убедиться, что N+1 отбит, а после освобождения слота проходит. Сверка
   concurrency_cap() с формулой остается, но пункт 6 ею НЕ закрывается.
3) ПОДМЕНА МОДЕЛИ В РАЗРЕЗЕ РАСХОДА. Сейчас она видна отдельной командой
   swarm_contract.py --substitutions, а пункт (д) просит ее в разрезе расхода;
   docs/remont/04-KONTRAKT-ROYA-M03.md:67 сам называет перенос невыполненным.
   Либо перенеси в token_ledger, либо обоснуй, почему разрез принадлежит
   swarm_contract - но не оставляй расхождение между документом и кодом.

ЗАЧТЕНО И НЕ ПЕРЕДЕЛЫВАТЬ: (а) блок контракта роя работает; (б) аудит подмены
модели ловит haiku против политики; (г) --accept fail-closed; (е) мутации прибора
краснеют; (ж) прибор не сирота - claude_guard.py:2185 зовет spawn_verdict и
исполняет через block(); (ж2) --audit-agents; (з) deny накрывает прибор.

## Промт проверяющего
ВЕРДИКТ: зеленый|красный
Отчет исполнителя не читай. Твоя песочница ТОЛЬКО ДЛЯ ЧТЕНИЯ: временный каталог тебе
не создать. check_cmd и probe_cmd гоняет ГЕЙТ, а не ты - враждебная проба строит сломанный
вход и потому принадлежит гейту. Не пытайся их запускать: красный на «mktemp: Operation not
permitted» будет ложным, а от ложных красных судью перестают читать.
Проверяй утверждения ниже своими средствами: чтением кода, грепом, запуском того, что
записи не требует. Сомнение трактуй в красную сторону. При красном назови точный путь и условие.

Отдельно проверь:
(а) спавн general-purpose с задачей поиска практики блокируется, а он же с задачей вне дела - проходит;
(б) --audit-agents краснеет, если подменить модель в заголовке одного агента (делай в копии);
(в) потолок шести держит код: попытка седьмого одновременного агента отбивается;
(г) агент без маркера на диске не считается закрытым;
(д) подмена модели харнессом видна отдельной строкой в разрезе расхода;
(е) селфтест краснеет по каждому из пунктов 1, 2, 6;
(ж) ГЛАВНОЕ: у swarm_contract.py есть НАСТОЯЩИЙ вызывающий - грепом по дому найди его.
    Прибор, которого никто не зовет, вердикт не получает;
(ж2) --audit-agents ЗЕЛЕНЫЙ на нынешнем исправном доме, и при этом краснеет, если
    временно убрать пин у одного из 13 файловых агентов либо оставить логическую роль
    без единого потребителя;
(з) новый прибор накрыт слоем защиты: python3 scripts/claude_guard.py --deny-covers
    scripts/swarm_contract.py отдает ноль.
